# Google Places API Configuration
# Required for fetching Google Reviews at build time

# Get your API key from Google Cloud Console:
# 1. Go to https://console.cloud.google.com/
# 2. Create a new project or select existing one
# 3. Enable "Places API" in APIs & Services
# 4. Go to Credentials and create an API key
# 5. Restrict the API key to "Places API" for security
GOOGLE_PLACES_API_KEY=your_api_key_here

# Find your Place ID:
# 1. Go to https://developers.google.com/maps/documentation/places/web-service/place-id
# 2. Search for "Cross Creeks Dental, 1555 Cross Creeks Blvd, Pickerington, OH"
# 3. Copy the Place ID (starts with "ChIJ...")
GOOGLE_PLACE_ID=your_place_id_here

# For production deployment via GitHub Actions:
# Add these as GitHub Secrets in your repository:
# Settings > Secrets and variables > Actions > New repository secret
# - GOOGLE_PLACES_API_KEY
# - GOOGLE_PLACE_ID
